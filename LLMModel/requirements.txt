# RAG + VectorDB (LLMModel)
chromadb>=0.4.0
sentence-transformers>=2.2.0
# PDF support (optional; project may already have it)
PyPDF2>=1.26.0

# Optional: for embedding_provider="gemini"
# google-generativeai

# Optional: for embedding_provider="openai"
# openai

# Optional: LangChain stack (RAGPipelineLangChain) - Gemini + Chroma + Document
# langchain-google-genai
# langchain-chroma
# langchain-core
